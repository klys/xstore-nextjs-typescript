This is a boilerplate come with nextjs with typescript

install it first with

yarn

and then

yarn add --dev typescript

or

yarn add --dev @types/react

and then you can just run the proyect with

yarn dev

build to deploy it with

yarn build

test the build with

yarn start
